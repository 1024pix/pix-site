<main class="page simple-page">

  <header class="page-header">
    <div class="container md padding-container">
      <h1 class="page-header__title">
        {{as-text model.rawJSON.title}}
      </h1>
    </div>
  </header>

  <main class="page-body">
    {{#unless (is-empty model.rawJSON.header)}}
      <div class="page-body__header container md padding-container">
        {{{as-html model.rawJSON.header}}}
      </div>
    {{/unless}}

    {{#unless (is-empty model.rawJSON.body)}}
      <div class="page-body__body container md padding-container">
        {{{as-html model.rawJSON.body}}}
      </div>
    {{/unless}}

    {{#unless (is-empty model.rawJSON.footer)}}
      <div class="page-body__footer container md padding-container">
        {{{as-html model.rawJSON.footer}}}
      </div>
    {{/unless}}
  </main>

</main>
