<div class="nav-switch">
  <div class="container padding-container">
    {{#each items as |item|}}
      <div><a class="text text-xs text-left text-up regular text-black" target="{{if item.primary.link.target "_blank"}}" href="{{as-url item.primary.link.url}}">{{as-text item.primary.title}}</a></div>
    {{/each}}
  </div>
</div>
